div
  div.row
    if !user
      div.messages-box-prompt
        a(view-link, href="/login") 登陆 
        | 后可留言
    if user
      form(ng-submit="submit()")
        fieldset
          legend  留言 
          div.large-12.columns
            label(for="content") {{label}}
            textarea(id="content", name="content", ng-model="data.content", rows=5, required)
          div.large-3.columns
            input.button.expand.secondary(type="button", value="取消", ng-click="cancel()", ng-show="current")
          div.large-3.columns
            input.button.expand(type="submit", value="发布")
    div.large-12.columns.panel(ng-repeat="msg in messages")
      p
        if user
          a.right(ng-click="response(msg, msg.user)") 回复 
        span {{msg.date | date:'yyyy-MM-dd hh:mm:ss'}} 
        b {{msg.user}} 说：
      p {{msg.content}}
      div.small-11.columns.right.panel.callout(ng-repeat="m in msg.responses")
        p
          if user
            a.right(ng-click="response(msg, m.user)") 回复 
          span {{m.date | date:'yyyy-MM-dd hh:mm:ss'}} 
          b {{m.user}} 
          | 回复： 
          b {{m.to}}
        p {{m.content}}